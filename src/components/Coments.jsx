import React, { Component } from 'react';

class Coments extends Component {
  render() {
    return (
      <div>
        <h4>{ email }</h4>
        <h4>{ ratting }</h4>
        <p>{ message }</p>
      </div>
    );
  }
}

export default Coments;
