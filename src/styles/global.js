import { createGlobalStyle } from 'styled-components';

const GlobalStyle = createGlobalStyle` /* stylelint-disable */
  * {
    margin: 0;
    padding: 0;
  }

  ul {
    list-style-type: none;
  }
`;

export default GlobalStyle;
